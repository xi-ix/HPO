# CHIP2026 任务一任务书：全文表型概念识别

> 版本：依据赛事公开任务说明整理  
> HPO 版本：v2026-06-23  
> 适用范围：子任务1（全文表型概念识别）
>
> /Users/wangzhe/Documents/Codex/2026-09-17/chip2026/outputs/CHIP2026_任务一任务书.md

## 1. 任务概述

任务一以 PMC 生物医学文献全文为输入，不区分患者，要求模型完成两件事：

1. 找出全文中的所有表型实体，并给出实体在原始全文中的准确位置；
2. 将每个实体标准化映射到人类表型本体（Human Phenotype Ontology，HPO）中的概念。

模型只使用 **HPO v2026-06-23 的 `Phenotypic abnormality (HP:0000118)` 分支**进行标准化。任务同时考察实体识别、实体边界判定和 HPO 概念标准化能力。

### 1.1 输入与输出

**输入：** 一篇结构化保存的 PMC 生物医学全文，包括文献编号、分章节全文、章节全局偏移量及患者信息等。

**任务一输出：** 文中全部表型实体组成的 `entities` 数组。每个实体至少包含：

- HPO 标识符；
- 实体类型；
- 全局字符起点；
- 字符长度；
- 原文文本；
- 否定标记。

任务一不要求判断表型属于哪位患者；患者与表型的关联属于任务二。

## 2. 数据集

数据集名称为 **PatientPheX**，来源于 PMC 公开发表的生物医学文献，包含全文级人工标注。

### 2.1 数据规模

| 统计项 | 训练集 | 开发集（A榜） | 测试集（B榜） | 总计 |
|---|---:|---:|---:|---:|
| 文献数 | 80 | 20 | 100 | 200 |
| 患者数 | 209 | 53 | 244 | 506 |
| 表型实体数 | 6,027 | 1,563 | 7,603 | 15,193 |
| 唯一 HPO ID 数 | 1,662 | 551 | 1,863 | 2,874 |
| 否定表型数 | 171 | 38 | 226 | 435 |
| 复合表型数 | 69 | 5 | 138 | 212 |
| 患者-表型关联数 | 2,307 | 563 | 2,786 | 5,656 |
| 患者相关唯一 HPO ID 数 | 1,091 | 351 | 1,252 | 1,995 |

其中，开发集和测试集均不提供标准答案，需在平台提交预测结果进行在线评测。

### 2.2 数据划分

- **训练集：** 80 篇，提供完整标注，用于训练、规则分析和本地验证；
- **A榜开发集：** 20 篇，不公开答案，用于在线调试，A榜成绩不计入最终成绩；
- **B榜测试集：** 100 篇，不公开答案，历史最优成绩用于最终排名。

A榜每队每日最多提交 5 次，B榜每队每日最多提交 3 次；评测失败不扣提交次数。排行榜整点更新。

## 3. 数据格式与字段

数据采用 **UTF-8 编码的 JSONL 格式**，每一行是一个完整、合法的 JSON 对象，对应一篇文献。

### 3.1 顶层结构

```json
{
  "pmc_id": "12652105",
  "pmid": "41300704",
  "patient": [],
  "full_text": [],
  "entities": [],
  "association": []
}
```

| 字段 | 类型 | 与任务一的关系 | 说明 |
|---|---|---|---|
| `pmc_id` | string | 必需 | PMC 全文编号 |
| `pmid` | string | 必需 | PubMed 文献编号 |
| `patient` | array | 输入参考 | 文中指定患者及部分有效指称，主要服务任务二 |
| `full_text` | array | 核心输入 | 按 passage/section 组织的全文 |
| `entities` | array | 核心标签/输出 | 全文中的表型实体 |
| `association` | array | 非任务一目标 | 患者与表型的关联，属于任务二 |

### 3.2 `full_text` 结构

```json
{
  "section_type": "TITLE",
  "type": "front",
  "offset": 0,
  "text": "Rare Duplication in the RYR1 Gene ..."
}
```

| 字段 | 含义 |
|---|---|
| `section_type` | 章节类别，如 `TITLE`、`ABSTRACT` |
| `type` | BioC passage 的细粒度类型，如 `front`、`abstract` |
| `offset` | 该 section 在原始完整全文中的全局起始字符位置 |
| `text` | section 的原始文本 |

处理全文时必须保留原始字符坐标。不要先删除空格、换行或改写标点再计算实体位置，也不要假设相邻 section 在字符串中无缝拼接；应以各 section 自带的 `offset` 为全局坐标依据。

若实体在某个 section 内的局部起点为 `local_start`，则：

```text
entity.offset = section.offset + local_start
entity.length = len(entity.text)
```

### 3.3 `entities` 结构

```json
{
  "identifier": "HP:0002047",
  "type": "Phenotype",
  "offset": 42,
  "length": 22,
  "text": "Malignant Hyperthermia",
  "note": null
}
```

| 字段 | 类型 | 规则 |
|---|---|---|
| `identifier` | string | 标准 HPO ID；复合表型用分号连接多个 ID；无有效 ID 时为 `-1` |
| `type` | string | 固定为 `Phenotype` |
| `offset` | integer | 实体在原始完整全文中的全局起始字符位置，按样例应视为从 0 开始 |
| `length` | integer | 实体原文的字符长度，不是 token 数或字节数 |
| `text` | string | 实体在原文中的实际文本，需与位置对应 |
| `note` | string/null | 普通表型为 `null`；明确否定的表型为 `NO` |

位置字段应满足：

```python
original_full_text[offset:offset + length] == text
```

由于输入按 section 保存，工程实现中也可按 section 的全局区间检查对应文本。具体全文还原方式应以官方数据读取代码或评测脚本为准。

## 4. 模型与资源要求

### 4.1 允许的方法

可以采用以下一种或多种方法：

- 词典匹配和规则系统；
- 传统机器学习；
- 深度学习和预训练语言模型；
- 大语言模型；
- 提示学习、监督微调、检索增强生成（RAG）等。

### 4.2 强制约束

- 使用的模型参数量不得超过 **10B**；
- HPO 标准化统一使用 **v2026-06-23**；
- 标准化范围限定为 `Phenotypic abnormality (HP:0000118)` 分支；
- 不允许使用外部人工标注的表型数据集；
- 技术报告中必须说明所使用的模型和外部知识资源。

可以使用 HPO 本体本身的术语、同义词、定义及层级信息作为知识资源，但应在技术报告中如实披露。若官方后续发布更具体的资源使用规则，以最新赛事通知为准。

### 4.3 官方 Baseline

任务一 Baseline 使用指定版本 HPO 训练的 **PhenoTagger** 进行表型识别和标准化。官方说明指出，整体 Baseline 未利用本比赛训练集做优化，因此训练集仍可用于边界规则学习、分类器训练及标准化排序等改进。

## 5. 提交格式

A榜和B榜均提交 UTF-8 编码的 JSONL 文件，格式与训练集一致。就任务一而言，核心预测字段是 `entities`：

```json
{
  "pmc_id": "...",
  "pmid": "...",
  "entities": [
    {
      "identifier": "HP:0000519",
      "type": "Phenotype",
      "offset": 206,
      "length": 30,
      "text": "bilateral congenital cataracts",
      "note": null
    }
  ],
  "association": []
}
```

赛事提交样例同时含有任务二的 `association` 字段。若平台要求两个子任务联合提交，应保留该字段并填入任务二结果；仅调试任务一时能否提交空数组，以平台校验规则为准。

## 6. 评测指标

任务一从 **实体提及级别**和**文档级别**两个粒度评价。官方只评价文献正文，不计算参考文献及其他非正文部分的预测结果。

### 6.1 实体提及级 F1（Mention-level F1）

一个普通表型预测只有同时满足以下条件才计为 TP：

1. 实体边界与金标准完全一致，即 `offset` 和 `length` 一致；
2. 标准化后的 HPO ID 正确。

仅文本有重叠、边界多一个或少一个字符、位置正确但 HPO ID 错误，均不算正确匹配。

计算公式：

```text
P_men  = TP / (TP + FP)
R_men  = TP / (TP + FN)
F1_men = 2 * P_men * R_men / (P_men + R_men)
```

### 6.2 文档级 F1（Document-level F1）

对每篇文档，将金标准和预测结果中的 HPO ID 分别去重，形成集合 `G_d` 和 `P_d`。文档级评价只关心某个 HPO 概念是否在文档中出现，不考虑实体位置、边界和出现次数。

```text
TP_doc = sum_d |G_d ∩ P_d|
FP_doc = sum_d |P_d - G_d|
FN_doc = sum_d |G_d - P_d|

P_doc  = TP_doc / (TP_doc + FP_doc)
R_doc  = TP_doc / (TP_doc + FN_doc)
F1_doc = 2 * P_doc * R_doc / (P_doc + R_doc)
```

这意味着同一 HPO ID 在一篇文档中出现多次，在文档级只计一次；但在实体提及级，每个出现位置都需正确识别。

### 6.3 特殊实体的计分规则

#### 否定表型

- 金标准中 `note="NO"` 的实体不是正例，漏掉它不计 FN；
- 若预测出该实体并正确标记 `note="NO"`，该实体不参与评价；
- 若将否定表型预测成普通表型，即未标 `NO`，计为 FP。

因此，识别否定表型的主要价值是阻止它被作为阳性表型提交。

#### 复合表型

若一个实体对应多个 HPO ID，`identifier` 使用分号分隔，例如：

```text
HP:xxxxxx;HP:yyyyyyy
```

评测时会按分号拆成多个独立评价单元并分别匹配。输出时不应使用逗号、空格或 JSON 数组代替官方规定的分号格式。

#### 无ID表型

无法映射到有效 HPO 概念时，`identifier` 为 `-1`。官方规定：无ID表型的预测实体位置信息正确即视为正确。

官方文字未进一步明确 `-1` 在文档级集合评价中的处理方式，应以官方评测脚本为准，本地评测实现不要自行推断。

### 6.4 对总成绩的贡献

完整比赛总分为：

```text
Score = 0.25 * (F1_men + F1_doc)
      + 0.25 * (F1_micro + F1_macro)
```

其中 `F1_micro` 和 `F1_macro` 属于任务二。由此可见，任务一的 `F1_men` 和 `F1_doc` 各占总分的 25%，任务一合计占总分的 50%。

## 7. 建议的系统范围

一个完整的任务一系统至少应包含以下模块：

```text
原始全文与坐标管理
        ↓
候选实体召回（HPO词典/PhenoTagger + NER）
        ↓
实体合并与边界修正
        ↓
否定与上下文判定
        ↓
HPO候选检索与上下文重排序
        ↓
复合表型、无ID表型处理
        ↓
JSONL生成与严格校验
```

模型开发应同时关注两个目标：

- **Mention-level：** 优先保证边界、位置和标准化 ID 同时正确；
- **Document-level：** 提高全文 HPO 概念召回，同时控制错误概念带来的 FP。

建议先复现官方 Baseline，再按“坐标正确性 → 边界学习 → 否定过滤 → 词典漏召回补充 → HPO消歧与重排序”的顺序迭代。

## 8. 本地验证与验收标准

### 8.1 数据切分

训练集只有 80 篇，建议按文档划分训练集与验证集，或进行文档级交叉验证。不得把同一篇文档拆到训练集和验证集两侧，否则会产生明显的数据泄漏。

### 8.2 必须报告的本地指标

- Mention Precision、Recall、F1；
- Document Precision、Recall、F1；
- 普通、否定、复合、`-1` 实体的分项结果；
- 边界错误、HPO ID 错误、漏检和误检的数量。

本地评测逻辑应尽量与官方规则一致；如官方发布评测脚本，应以官方脚本为唯一基准。

### 8.3 提交前检查清单

- [ ] 文件是 UTF-8 编码的合法 JSONL，每行一篇文献；
- [ ] 文献数量与待测数据一致，无缺失、重复或额外文献；
- [ ] `pmc_id`、`pmid` 与输入文献一致；
- [ ] 每个实体均包含 `identifier`、`type`、`offset`、`length`、`text`、`note`；
- [ ] `type` 为 `Phenotype`；
- [ ] `offset` 和 `length` 为整数且范围有效；
- [ ] 实体位置对应的原文切片与 `text` 完全一致；
- [ ] 普通实体的 `note` 为 `null`，否定实体为字符串 `NO`；
- [ ] HPO ID 属于指定版本和指定分支，或按规则填写 `-1`；
- [ ] 复合 HPO ID 使用英文分号 `;` 分隔；
- [ ] 未因清洗文本、Unicode替换、空白归一化或分词改变全局坐标；
- [ ] 未把参考文献及其他非正文内容中的实体作为普通预测提交；
- [ ] `entities` 中不存在意外的完全重复项；
- [ ] 联合提交时 `association` 字段满足平台格式要求。

## 9. 规则边界与待确认事项

以下细节在当前公开文字中没有完全定义，实施时应以官方数据、样例、答疑和评测脚本为准：

1. 跨 section 实体是否存在，以及其文本切片的官方还原方式；
2. Unicode 字符和换行在官方评测程序中的计数方式；
3. `identifier=-1` 是否参与文档级 HPO 集合评价；
4. 同一边界提交多个候选 HPO ID（非金标准复合写法）时的具体惩罚方式；
5. 仅提交任务一时，`association` 是否允许为空或省略；
6. 正文、参考文献及“其他非正文内容”的精确 passage 类型白名单。

在这些事项未明确前，建议保持原始文本不变、严格复制官方字段形式，并使用官方 Baseline 的数据读取和输出逻辑作为兼容性参照。

