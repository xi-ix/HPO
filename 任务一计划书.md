# 生物医学论文临床表型HPO标准化任务书

## 第一段：表型检测与HPO映射（无LLM版本）

---

### 一、任务范围界定

本任务书仅覆盖**第一段：表型检测与HPO映射**。

- **输入**：生物医学论文全文。
- **输出**：文中出现的临床表型 mention 及其对应的 HPO 编号，附带置信度与来源标记。
- **不涉及**：第二段“患者匹配”任务。
- **约束**：**不使用任何生成式大语言模型**（如 Qwen、GPT、LLaMA 等）。仅使用判别式预训练语言模型（BioBERT、SapBERT 等）、向量检索和规则/统计方法。
- **总参数量**：约 **0.22B**（BioBERT 110M + SapBERT 110M），远低于 10B 约束。
- **幻觉率**：最终 HPO ID 必须 100% 来自 HPO 官方本体。

---

### 二、项目目标

1. 构建“表型 mention 抽取 → 双通道 HPO 映射 → 规则交叉验证”的无 LLM 流水线。
2. 在自建测试集上，HPO 映射 **精确率 ≥ 0.75，召回率 ≥ 0.68，F1 ≥ 0.71**。
3. 双通道交叉验证相比任一单通道，**F1 提升 ≥ 3%**。
4. 否定检测准确率 **≥ 0.88**；家族史过滤准确率 **≥ 0.85**。
5. 输出结果可追溯到符号通道、检索通道或规则仲裁，并附带置信度。

---

### 三、总体架构

```
论文全文
   │
   ▼
[模块 1] 表型 mention 抽取
   │  PhenoTagger 词典 + BioBERT（~110M）
   │  输出：mention + PhenoTagger HPO ID + 概率
   ▼
[模块 2] 密集检索映射
   │  SapBERT/BioLORD（~110M） + FAISS 向量库
   │  输出：Top-K HPO 候选 + 相似度分数
   ▼
[模块 3] 规则交叉验证与仲裁
   │  分数融合 + 层级规则 + 否定/家族史规则
   ▼
最终输出：mention + HPO ID + 置信度 + 来源
```

**总参数量**：BioBERT 110M + SapBERT 110M ≈ **0.22B**，满足 <10B 约束，且完全不依赖生成式 LLM。

---

### 四、任务分解

#### 4.1 数据准备与向量库构建

| 编号 | 任务 | 具体内容 | 输出 |
|---|---|---|---|
| 4.1.1 | HPO 本体解析 | 下载 HPO 2024 官方文件，提取术语名称、同义词、定义、祖先层级、替代 ID | HPO 结构化表 |
| 4.1.2 | 嵌入模型选型 | 选用 **SapBERT** 或 **BioLORD**；如检索精度不足，可在 HPO 本体上做对比学习微调 | 嵌入模型权重 |
| 4.1.3 | 向量库构建 | 对每个 HPO 术语生成多条向量（名称、同义词、定义），用 FAISS 建索引 | FAISS 索引 + 映射表 |
| 4.1.4 | 测试集构建 | 从已标注病例报告中构建测试集，含 mention、正确 HPO ID、否定标记、家族史标记、粒度标注 | 测试集（≥300 条） |
| 4.1.5 | 交叉验证子集 | 抽取 ≥100 条，标注“符号通道正确/检索通道正确/两者均对/两者均错” | 交叉验证评估子集 |

#### 4.2 模块 1：表型 mention 抽取 + 符号通道

| 编号 | 任务 | 具体内容 | 输出 |
|---|---|---|---|
| 4.2.1 | 部署 PhenoTagger | 保留 mention 抽取和 HPO ID 多标签分类输出，记录每个 HPO ID 概率 | mention + HPO ID + 概率 |
| 4.2.2 | mention 规范化 | 去重、边界修正、组合表型拆分（如“智力障碍伴癫痫”拆为两个 mention） | 规范化 mention |
| 4.2.3 | 符号通道整理 | 对每个 mention，按概率排序整理 HPO ID，标记 Top-1 及概率 | 符号通道结果 |
| 4.2.4 | 否定/家族史预标记 | 用规则或轻量分类器标记否定、家族史语境 | 预标记结果 |

#### 4.3 模块 2：密集检索映射（检索通道）

| 编号 | 任务 | 具体内容 | 输出 |
|---|---|---|---|
| 4.3.1 | 在线编码 | 用 SapBERT 对每个 mention 编码 | mention 向量 |
| 4.3.2 | Top-K 检索 | 在 FAISS 中检索 Top-K HPO（建议 K=5~10） | 候选 HPO + 相似度 |
| 4.3.3 | 阈值过滤 | 低于相似度阈值的标记“检索无结果” | 检索通道结果 |
| 4.3.4 | 层级扩展 | 对 Top-K 补充祖先/后代术语（限 2 层），用于粒度调整 | 扩展候选集 |

#### 4.4 模块 3：规则交叉验证与仲裁（无 LLM）

##### 4.4.1 候选并集构建

对每个 mention，合并以下候选：
- 符号通道输出的所有 HPO ID；
- 检索通道 Top-K；
- 上述术语的祖先/后代（限 2 层）。

##### 4.4.2 分数融合

对候选并集中每个 HPO ID，计算综合分：

```
score = α · p_sym_norm + β · sim_norm + γ · 层级一致性 + δ · 字符串/同义词重叠
```

- `p_sym_norm`：PhenoTagger 概率归一化；
- `sim_norm`：检索相似度归一化；
- `层级一致性`：候选与两通道 Top-1 是否一致或为祖先/后代；
- `字符串/同义词重叠`：mention 与 HPO 术语名称/同义词的字符或词重叠度。
- 权重 α、β、γ、δ 在验证集上网格搜索确定。

##### 4.4.3 一致性分类与仲裁规则

| 情况 | 定义 | 处理方式 |
|---|---|---|
| **强一致** | 符号 Top-1 = 检索 Top-1 | 直接采纳，置信度高 |
| **弱一致** | 符号 Top-1 在检索 Top-K 中，或两者为祖先/后代 | 优先选最特异术语；若最特异综合分低于宽泛术语一定幅度，则选宽泛术语；置信度中 |
| **冲突** | 符号 Top-1 不在检索 Top-K，且无层级关系 | 选综合分最高者；若最高分 < T_low，标记“无法映射”；置信度低 |
| **单通道有结果** | 仅一个通道有有效输出 | 若该通道置信度 > T_single，采纳；否则低置信 |
| **双通道均无结果** | 两者均无有效输出 | 标记“无法映射” |

##### 4.4.4 否定与家族史处理（规则/轻量分类器）

| 编号 | 任务 | 具体内容 | 输出 |
|---|---|---|---|
| 4.4.4.1 | 否定检测 | 使用 NegEx/BioNeg 规则 + 微调 BioBERT 分类器（可选）；否定 mention 丢弃 HPO 映射，但保留否定标记 | 否定标记 |
| 4.4.4.2 | 家族史过滤 | 规则 + 分类器判断是否仅出现在家族史描述中，按项目需求保留或单独标记 | 家族史标记 |
| 4.4.4.3 | 粒度调整 | 利用 HPO 层级与信息含量（IC），在候选并集中选择最特异且综合分达标者 | 最终 HPO ID |

##### 4.4.5 结果合并

输出格式：

```json
{
  "mention": "严重的精神运动发育迟缓",
  "hpo_id": "HP:0001263",
  "confidence": "高",
  "source": "强一致",
  "negated": false,
  "family_history": false
}
```

---

### 五、评估方案

#### 5.1 主评估

| 指标 | 目标值 |
|---|---|
| HPO 映射精确率 | ≥ 0.75 |
| HPO 映射召回率 | ≥ 0.68 |
| HPO 映射 F1 | ≥ 0.71 |
| 双通道交叉验证 F1 提升 | ≥ 3%（相比单通道） |
| 规则仲裁准确率 | ≥ 0.75 |
| 否定检测准确率 | ≥ 0.88 |
| 家族史过滤准确率 | ≥ 0.85 |
| 幻觉率 | 0% |

#### 5.2 对比实验

| 组别 | 说明 |
|---|---|
| PhenoTagger 单独 | 符号通道基线 |
| 密集检索单独 | 检索通道基线 |
| 双通道交叉验证 | 本方案 |
| 消融：去符号通道 | 仅检索通道 + 规则 |
| 消融：去检索通道 | 仅符号通道 + 规则 |
| 消融：去层级后处理 | 评估层级规则贡献 |

#### 5.3 一致性分析

统计强一致、弱一致、冲突、单通道、无法映射的比例，并分析规则仲裁对最终 F1 的贡献。

---

### 六、交付物

1. **代码仓库**：模块 1、2、3 完整实现、配置文件和运行脚本。
2. **HPO 向量库**：FAISS 索引 + 术语映射表 + 嵌入模型权重。
3. **测试集与标注**：≥300 条 mention，含正确 HPO ID、否定标记、家族史标记、粒度标注。
4. **交叉验证评估子集**：≥100 条，标注双通道正确性。
5. **评估报告**：主评估、对比实验、一致性分析、消融实验。
6. **技术文档**：架构说明、部署指南、API 接口文档。
7. **任务书验收报告**：对照验收标准逐项说明。

---

### 八、资源需求

| 资源类型 | 需求 | 说明 |
|---|---|---|
| 模型 | PhenoTagger BioBERT（~110M） | 符号通道 + mention 抽取 |
| 模型 | SapBERT 或 BioLORD（~110M） | 检索通道嵌入模型 |
| 算力 | 1× GPU（≥8GB 显存） | 支持 BioBERT/SapBERT 推理与微调 |
| 数据 | HPO 2024 官方本体 | 公开可用 |
| 数据 | 生物医学论文全文 | PubMed Central 或项目提供 |
| 工具 | FAISS、Transformers、PyTorch、NegEx/BioNeg | 开源 |

---

### 九、验收标准

| 指标 | 目标值 | 测量方式 |
|---|---|---|
| HPO 映射精确率 | ≥ 0.75 | 测试集 |
| HPO 映射召回率 | ≥ 0.68 | 测试集 |
| HPO 映射 F1 | ≥ 0.71 | 测试集 |
| 双通道 F1 提升 | ≥ 3% | 相比单通道 |
| 规则仲裁准确率 | ≥ 0.75 | 仲裁样本 |
| 否定检测准确率 | ≥ 0.88 | 测试集 |
| 家族史过滤准确率 | ≥ 0.85 | 测试集 |
| 幻觉率 | 0% | 输出 HPO ID 全部在官方本体中 |
| 总参数量 | ≤ 0.3B | BioBERT + SapBERT |
| 不使用 LLM | 是 | 架构验证 |

---

### 十、风险与应对

| 风险 | 影响 | 应对措施 |
|---|---|---|
| 嵌入模型领域适应不足 | 检索精度低 | 在 HPO 本体上做对比学习微调；尝试多个嵌入模型 |
| 双通道同错 | 交叉验证失效 | 统计同错率；对高置信同错样本人工抽查；考虑引入第三规则通道 |
| 组合表型拆分错误 | 漏映射或错映射 | 增加规则拆分；对拆分后 mention 分别检索 |
| 否定检测漏检 | 假阳性映射 | 结合 NegEx 规则与 BioBERT 分类器；增加规则兜底 |
| 粒度选择不当 | 映射到过宽或过窄术语 | 利用 HPO 层级与 IC 值；规则仲裁优先最特异术语 |
| 冲突仲裁规则不优 | F1 不达标 | 在验证集上网格搜索分数融合权重与阈值 |
| HPO 本体更新 | 向量库过期 | 设计增量更新流程，仅重新编码新增术语，无需重新训练模型 |

---

### 十一、备注

本任务书仅覆盖第一段“表型检测与HPO映射”，不包含第二段“患者匹配”。全流程不使用任何生成式大语言模型，仅依赖判别式预训练模型（BioBERT、SapBERT）、向量检索和规则/统计方法。总参数量约 0.22B，满足 <10B 约束。最终输出的 HPO ID 全部来自 HPO 官方本体，幻觉率为 0%。该方案适合表型描述相对规范、追求映射确定性和可解释性的论文场景。